<%= render :partial => 'shared/modal_toolbar' %>
<div>
  <h1>Edit <%= @event.name %></h1>
  <%= form_for @event, :url => event_path(@event.id), :method => :post, :remote => true do |form| -%>
  <%= render :partial => 'form', :object => form %>
  <%= form.submit "Update" %>
  <% end -%>
</div>
