<%= render :partial => 'shared/modal_toolbar' %>
<div>
  <h1>New project</h1>
  <%= form_for :project, :url => projects_path, :method => :post, :remote => true do |form| -%>
  <%= render :partial => 'form', :object => form %>
  <%= form.submit "Save" %>
  <% end -%>
  <%= link_to 'Back', projects_path, :remote => true %>
</div>
